<div class="homePage__container">
    <div class="homePage__card">
        <img src="/logo.png" alt="Lab logo" width="110px" height="120px">
        <h2 style="color: var(--text)">Welcome to the Web Security Demo</h2>
        <p style="color: var(--muted-text)">This is a demonstration of Referrer-based access control.</p>
        <% if (!user) { %>
            <p style="color: var(--muted-text)">
            Please 
            <a href="/login" style="color: var(--primary); text-decoration: underline;">login</a> 
            to access your profile.
            </p>
        <% } %>
    </div>
</div>
