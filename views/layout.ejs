<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 min-h-screen">
    <nav class="bg-white shadow-lg">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16">
                <div class="flex space-x-8">
                    <a href="/" class="inline-flex items-center px-1 pt-1 text-gray-900 hover:text-blue-600 transition-colors duration-200 font-bold">Home</a>
                    <% if (user) { %>
                        <a href="/profile" class="inline-flex items-center px-1 pt-1 text-gray-900 hover:text-blue-600 transition-colors duration-200">Profile</a>
                        <% if (user.role === 'admin') { %>
                            <a href="/admin" class="inline-flex items-center px-1 pt-1 text-gray-900 hover:text-blue-600 transition-colors duration-200">Admin Panel</a>
                        <% } %>
                        <a href="/logout" class="inline-flex items-center px-1 pt-1 text-gray-900 hover:text-blue-600 transition-colors duration-200">Logout</a>
                    <% } else { %>
                        <a href="/login" class="inline-flex items-center px-1 pt-1 text-gray-900 hover:text-blue-600 transition-colors duration-200">Login</a>
                        <a href="/singup" class="inline-flex items-center px-1 pt-1 text-gray-900 hover:text-blue-600 transition-colors duration-200">Singup</a>
                    <% } %>
                </div>
            </div>
        </div>
    </nav>
    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <%- body %>
    </main>
</body>
</html> 